server :
    port : 10000
spring :
    application :
        name : joomall-product
    datasource :
        driver-class-name : com.mysql.cj.jdbc.Driver
        username : root
        password : 88888888
        url : jdbc:mysql://127.0.0.1:3306/joomall_product?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    cloud :
        nacos :
            discovery :
                server-addr : 127.0.0.1:8848
# MapperScan sql.xml映射文件位置
mybatis-plus :
    mapper-locations : classpath:/mapper/**/*.xml
    global-config :
        db-config :
            id-type : auto
            logic-delete-value: 1 # 表示已经删除
            logic-not-delete-value: 0  # 表示没有删除
logging:
    level:
        com.jaron.joomall: debug